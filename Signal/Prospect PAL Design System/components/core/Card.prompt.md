One-line: the surface primitive — 1px hairline border, 12px radius, soft two-layer shadow.

```jsx
<Card interactive pad={24}><h3>Workflow Builder</h3></Card>
```

Cards never carry a coloured left border. Use `tone="deep"` inside navy canvas sections.
