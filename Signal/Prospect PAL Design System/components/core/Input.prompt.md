One-line: single-line text field; 1.5px hairline border, cobalt focus ring.

```jsx
<Input icon="link" mono placeholder="https://your-instance.n8n.cloud" />
```

The intake form never asks for a secret value — only which provider. Use `mono` for anything machine-shaped.
