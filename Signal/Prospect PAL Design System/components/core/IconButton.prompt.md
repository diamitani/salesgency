One-line: a square glyph-only control for toolbars, top bars and card corners.

```jsx
<IconButton icon="circle-help" label="Help" />
<IconButton icon="x" label="Close" variant="ghost" />
```

Use `variant="deep"` on navy surfaces. Always pass `label`.
