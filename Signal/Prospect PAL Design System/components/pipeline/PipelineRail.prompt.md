One-line: the horizontal node graph — NodeCards separated by chevrons, horizontally scrollable, one selected.

```jsx
<PipelineRail nodes={NINE_NODES} activeIndex={2} onSelect={setActive} />
```

Never wrap the rail onto two lines; it scrolls.
