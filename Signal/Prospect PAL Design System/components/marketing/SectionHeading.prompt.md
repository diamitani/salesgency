One-line: the marketing section header — cobalt uppercase eyebrow, tight 44px display title, one supporting sentence.

```jsx
<SectionHeading eyebrow="Production artifacts" title="What a compile hands you" description="Six deliverables, ready to import." />
```

One eyebrow per section, sentence case titles, never more than one supporting paragraph.
