One-line: a plan column; exactly one card in a row may be `featured`.

```jsx
<PricingCard featured name="Pro unlimited engine" price="$99" cadence="/ month" features={[...]} cta={<Button variant="accent" fullWidth>Start Pro</Button>} />
```
