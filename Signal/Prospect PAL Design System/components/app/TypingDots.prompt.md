One-line: the agent-thinking indicator — three cobalt dots plus a plain-language label.

```jsx
<TypingDots label="Resolving tool bindings" />
```

Always label what the agent is doing; never a bare spinner.
