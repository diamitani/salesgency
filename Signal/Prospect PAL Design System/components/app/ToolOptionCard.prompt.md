One-line: a selectable stack choice in the intake wizard (trigger kind, CRM, sequencer, LLM provider).

```jsx
<ToolOptionCard icon="database" title="HubSpot" meta="OAuth2" caption="CRM dedupe + contact upsert" selected />
```

Selected state = cobalt 1.5px border, cobalt-50 fill, focus ring, check glyph. Show `meta` so the auth mode is visible before deploy.
