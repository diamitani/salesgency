One-line: one row of the navy workspace sidebar; the active row gets a champagne inset rule, never a coloured border-left card.

```jsx
<NavItem icon="workflow" label="Workflow Builder" active />
<NavItem icon="radio" label="Tech Signals" badge="1.4k" />
```
