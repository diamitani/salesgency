One-line: centred overlay for checkout, deploy-connect and demo tours; scrim is ink at 55% with a 6px blur.

```jsx
<Modal eyebrow="Connect & deploy" title="Push to your n8n instance" footer={<Button variant="accent">Deploy</Button>} />
```

Use `tone="deep"` for canvas/architecture overlays.
