One-line: a single hard number with an uppercase caption — dashboard stat rows and marketing proof strips.

```jsx
<StatTile value="100" unit="% guard" label="Dedupe accuracy" tone="verified" />
```

Four per row maximum. Every number must be real; no decorative stats.
